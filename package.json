{
  "name": "me-cook-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "web:dev": "cd apps/web && pnpm run start",
    "api:dev": "cd apps/api && pnpm run dev",
    "test": "pnpm -r test",
    "types:gen": "supabase gen types typescript --linked > packages/db-types/src/generated.ts && echo '✅ generated db types into packages/db-types/src/generated.ts'"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.2"
  }
}
